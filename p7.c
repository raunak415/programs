//copy one array to another using pointer.
#include<stdio.h>
int main(){
    int i,a[5],b[5];
    int *p=a;
    for(i=0;i<n;i++){
        b[i]=a[i];
    }
        printf("enter elements :");
    for(i=0;i<=5;i++){
        scanf("%d",&a[i]);
   b[i]=a[i];
    }
   printf("copy array");
   printf("%d",b[i]);
   return 0;
}